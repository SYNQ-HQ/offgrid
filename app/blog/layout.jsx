export const metadata = {
  title: "OffGrid Blog | Stories & Updates",
  description: "Insights and updates from the OffGrid community. Disconnect to Reconnect.",
};

export default function BlogLayout({ children }) {
  return children;
}
